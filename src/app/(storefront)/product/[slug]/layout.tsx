/**
 * Product pages use the standard storefront layout (with Header + Footer).
 * This file just passes through — no overrides.
 */
export default function ProductLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
